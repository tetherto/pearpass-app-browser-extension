/**
 * Cryptographic algorithm constants
 */
export const CRYPTO_ALGORITHMS = Object.freeze({
  SHA_256: 'SHA-256',
  PBKDF2: 'PBKDF2',
  AES_GCM: 'AES-GCM',
  ECDSA: 'ECDSA'
})

/**
 * Elliptic curve constants
 */
export const ELLIPTIC_CURVES = Object.freeze({
  P_256: 'P-256'
})
